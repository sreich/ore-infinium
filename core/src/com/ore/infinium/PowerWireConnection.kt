package com.ore.infinium

/**
 * Each circuit is composed of >= 1 wire connections, each wire connection is composed of
 * only 2 different devices.
 */
class PowerWireConnection(var firstEntity: Int, var secondEntity: Int) {
}
